import React from 'react'
import Layout from '../components/layout'

const work = () => (
        <Layout>
            <p>This is the page for my work history.</p>
        </Layout>
    )

export default work
